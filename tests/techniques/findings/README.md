# Findings

One file per technique suite, written by that suite's author and nobody else.
Each entry names the file, how to reproduce it, and what was expected against
what happened. These are evidence: nothing in `src/` or `db/` was changed to
accommodate any of them.
