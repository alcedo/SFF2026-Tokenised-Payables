# Findings

Defects and surprises the technique suites turned up. One section per finding.
Each names the file, how to reproduce it, and what was expected against what
happened. A finding here is evidence, not a to-do list: nothing in `src/` or
`db/` was changed to accommodate it.
